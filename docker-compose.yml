services:
  dev:
    build: 
      context: .
      target: dev
    volumes:
      # (1)
      - type: bind
        source: ./src
        target: /src
      # (2)
      - type: volume
        source: node_modules
        target: /src/node_modules
    ports:
      - "3000:3000"
    tty: true
    stdin_open: true

volumes:
  node_volumes:
